const candidateRoutes = require('./candidate.route');
const candidateController = require('./candidate.controller');
const candidateService = require('./candidate.service');

module.exports = {
  candidateRoutes, candidateService, candidateController,
};
